import { BigNumber } from 'ethers';

export const ONE_YEAR_IN_SECONDS = BigNumber.from(31536000);
export const BASE = BigNumber.from(10).pow(18);
export const MAX_UINT256 = BigNumber.from(2).pow(255);
export const CREDIT_PROOF_PROTOCOL = 'arcx.credit';
