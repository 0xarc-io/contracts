import { BigNumber } from 'ethers';

export let TEN_PERCENT = BigNumber.from(3022265980);
export let FIVE_PERCENT = BigNumber.from(1547125957);
export let ONE_YEAR_IN_SECONDS = BigNumber.from(31536000);
export let BASE = BigNumber.from(10).pow(18);
export let MAX_UINT256 = BigNumber.from(2).pow(255);
