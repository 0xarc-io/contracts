import { ITestContext } from './d1ArcDescribe';

export interface ArcOptions {}

/**
 * Initialize the Arc smart contracts
 */
export default async function initializeArc(
  ctx: ITestContext,
  options: ArcOptions = {},
): Promise<void> {}
